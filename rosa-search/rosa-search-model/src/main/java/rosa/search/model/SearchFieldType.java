package rosa.search.model;

/**
 * Hints for how the search serviced should interpret a search field.
 */
public enum SearchFieldType {
    OLD_FRENCH, ENGLISH, FRENCH, STRING, IMAGE_NAME, ITALIAN, GREEK, SPANISH, LATIN, GERMAN;
}
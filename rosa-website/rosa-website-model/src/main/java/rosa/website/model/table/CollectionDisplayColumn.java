package rosa.website.model.table;

/**
 * Columns for Table displaying information about a collection.
 */
public enum CollectionDisplayColumn {
    NAME, DATE, FOLIOS, ILLUSTRATIONS, COLUMNS, LINES, SIZE, STRUCT, MORE_THAN_ONE_ILLUSTRATION
}